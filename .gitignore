# This is the .gitignore file.
# Citation:
# Adapted From: Python.gitignore in the github/gitignore repository
# Author: github
# Date Retrieved: 05/08/2020

# These are the byte-compiled, optimized, and DLL files to ignore.
__pycache__/
*.py[cod]
*$py.class

# This is the C extension files to ignore.
*.so

# These are the distribution, and packaging to ignore.
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller: Usually these files are written by a python script from a template before PyInstaller builds the exe, so
# as to inject date/other infos into it.
*.manifest
*.spec

# These are the installer logs to ignore.
pip-log.txt
pip-delete-this-directory.txt

# These are the unit tests, and coverage reports to ignore.
htmlcov/
.tox/
.nox/
.coverage
.coveralls.yml
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# These are the translations to ignore.
*.mo
*.pot

# These are the Django files to ignore.
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# These are the flask files to ignore.
instance/
.webassets-cache

# This is the scrapy files to ignore.
.scrapy

# This is the sphinx files to ignore.
docs/_build/

# These are the PyBuilder files to ignore.
.pybuilder/
target/

# This is the Jupyter Notebook files to ignore.
.ipynb_checkpoints

# These are the IPython files to ignore.
profile_default/
ipython_config.py

# These are the environmental files to ignore.
.env*
.venv*
env/
venv/
/venv/
ENV/
env.bak/
venv.bak/
/venvTest/
/venvTest3.7/
/venvTest3.8/
/venvTest3.9/

# These are the spyder project setting files to ignore.
.spyderproject
.spyproject

# This is the rope project settings files to ignore.
.ropeproject

# This is the mkdocs documentation file to ignore.
/site

# These are the mypy files to ignore.
.mypy_cache/
.dmypy.json
dmypy.json

# This is the pyre type checker file to ignore.
.pyre/

# This is the pytype static type analyzer to ignore.
.pytype/

# This is the cython debug symbols to ignore.
cython_debug/

# These are folders for scripts in development to ignore.
/testing_in_development/
/experimental/

# These are folders created by testing to ignore.
/tests/_trial_temp/
/tests/integration/_trial_temp/
/tests/integration/fixtures/_trial_temp/
/tests/unit/_trial_temp/
/tests/unit/fixtures/_trial_temp/

# These are folders and files created by releasing.
/build
/dist
/Output
.spec

# These are files and folders created by usage to ignore
Animate.webp
Draw.webp
* Forces.png
* ForceCoefficients.png
* Moments.png
* MomentCoefficients.png
