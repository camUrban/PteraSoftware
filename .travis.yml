language: python
python:
  - "3.7"
os: linux
dist: xenial
install:
  - pip install -r REQUIREMENTS.txt
script:
  - coverage run --source=asmvp -m unittest discover -s tests
after_success:
  - coveralls --service=travis-pro
